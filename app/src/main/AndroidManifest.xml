<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="enthusiast.yzw.shift_arrangement_helper">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ShowShift"></activity>
        <activity android:name=".activities.AddOrEditShift" />
        <activity android:name=".activities.EditAbsentRemain" />
        <activity android:name=".activities.AbsentRemain" />
        <activity android:name=".activities.BedAssignActivity" />
        <activity android:name=".activities.AddOrEditPerson" />
        <activity android:name=".activities.PersonManage" />
        <activity android:name=".activities.AddOrEditWorkCategory" />
        <activity android:name=".activities.WorkCategoryManage" />
        <activity android:name=".activities.AddOrEditBed" />
        <activity android:name=".activities.BedManage" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>